[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "kindle-display"
version = "0.1.0"
description = "Low-frequency status dashboards for a Kindle 4"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
